# === SECRETS — never commit ===
.env
.env.*
*.env
pm-hl-conservative-plus-repo/.env
pm-hl-conservative-plus-repo/.env.*

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/
pm-hl-conservative-plus-repo/.venv/

# === macOS ===
.DS_Store
.AppleDouble
.LSOverride

# === Runtime state (pidfiles + scratch only — keep logs + CSV history) ===
skills/btc-5m-live/runtime/*.pid
skills/btc-5m-live/runtime/par_*.pid

# === Editor / IDE ===
.vscode/
.idea/
*.swp
*.swo
*~

# === Scratch / tmp ===
/tmp/
*.tmp